#ifndef __TASK_COMPRESSOR_H
#define __TASK_COMPRESSOR_H

/* ===========================================================================
 * 逻辑2: 压缩机开机逻辑
 *
 * 流程 (来源: 1.1温度控制流程.pdf 右上逻辑图):
 *   1. 开启压缩机, 设初始频率 F = 125 Hz
 *   2. 等待热车时长 C20 到时
 *   3. C20 到时后 → 进入 PID 调整模块
 *   4. C20 未到时 → 继续等待
 * =========================================================================== */

void Task_Compressor_Process(void const *argument);

#endif /* __TASK_COMPRESSOR_H */
